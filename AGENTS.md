# Agent guide: launch-kit

To install for the user, follow the section for your runtime in `README.md` → Install.
The canonical skill is `skills/launch-kit/SKILL.md`; `.cursor/skills/launch-kit` is a symlink to it.
To run it without installing: read `skills/launch-kit/SKILL.md` and follow it on the user's repo.
Only write inside the user's repo (`launch/` folder). Never post anything online on the user's behalf.
